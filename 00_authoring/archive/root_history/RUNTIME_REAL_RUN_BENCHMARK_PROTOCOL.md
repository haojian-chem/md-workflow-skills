# LEGACY — Runtime real-run benchmark protocol

Status: LEGACY / HISTORY ONLY

This benchmark protocol targets the historical runtime manifest / route-fast-path / AGENT_TASK architecture and must not be used as a current Lightweight Runtime acceptance protocol.

Current runtime authority:

`00_authoring/lightweight_runtime_v2_spec.md`

Current implementation/validation status:

```text
00_authoring/SYNC_STATUS.md
00_authoring/skill_inventory.yaml
00_authoring/file_ownership.yaml
```

The historical benchmark remains useful only as evidence that excessive runtime overhead came primarily from LLM-side orchestration rather than deterministic filesystem/tool execution.

Original content remains available in Git history. Previous blob SHA:

`f99b79b9f647736078ef0e481c523d6c2bd0b7a8`
