---
name: <validator-skill-name>
description: <检查或分类任务及排除边界>。
---

# 目标

# 职责边界

# 输入

# 检查规则

# Findings 与 gate

# 统一确认项

先完成全部可处理对象，再汇总确认事项。

# 详细报告

# 返回

返回必须符合：

`03_contracts/subagent_result.schema.yaml`

不得修改被验证目标，不得创建其他子 Agent，不得直接向用户提问。
