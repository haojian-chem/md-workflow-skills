# SUPERSEDED — legacy four-layer responsibility model

Status: SUPERSEDED / HISTORY ONLY

本文件原先把科研 Skill 强制组织为：

```text
Workflow
Operation
Validator
```

这不再是当前 authoring 模型。

当前规则：

```text
main Skill
+ references when detail is long
+ supporting Skill only when a complex, clear boundary justifies it
+ deterministic Tool for deterministic capability
```

当前 authority：

`00_authoring/md-workflow-skill-authoring/references/skill_boundaries.md`

现存 `01_workflows/`、`02_operations/`、`02_validators/` 只是历史布局/迁移中的路径，不得据此强制分类新 Skill。

原文件内容保留在 Git history。Previous blob SHA:

`a6d29062be5fcfc8c175e8ab193b9335b1f98dcf`
