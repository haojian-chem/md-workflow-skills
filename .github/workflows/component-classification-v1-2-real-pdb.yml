name: component-classification-v1-2-real-pdb

on:
  pull_request:
    paths:
      - "01_structure_preparation/1.2_component_and_residue_classification/**"
      - "04_evals/component_and_residue_classification_validator/test_v1_2_real_pdb_acceptance.py"
      - ".github/workflows/component-classification-v1-2-real-pdb.yml"
  workflow_dispatch:

jobs:
  real-pdb-acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r 01_structure_preparation/1.2_component_and_residue_classification/scripts/requirements.txt
          python -m pip install pytest
      - name: Download official RCSB PDB entries
        run: |
          set -euo pipefail
          mkdir -p real_pdb_inputs
          for pdb_id in 1VNS 1A6M 1CRN; do
            curl --fail --location --retry 5 --retry-all-errors \
              --output "real_pdb_inputs/${pdb_id}.pdb" \
              "https://files.rcsb.org/download/${pdb_id}.pdb"
          done
          sha256sum real_pdb_inputs/*.pdb | tee real_pdb_inputs/sha256sums.txt
          {
            echo "downloaded_at_utc=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "source_1VNS=https://files.rcsb.org/download/1VNS.pdb"
            echo "source_1A6M=https://files.rcsb.org/download/1A6M.pdb"
            echo "source_1CRN=https://files.rcsb.org/download/1CRN.pdb"
          } | tee real_pdb_inputs/provenance.txt
      - name: Run real PDB acceptance
        env:
          V1_2_REAL_PDB_DIR: ${{ github.workspace }}/real_pdb_inputs
        run: |
          set -o pipefail
          mkdir -p real_pdb_outputs
          pytest -q \
            04_evals/component_and_residue_classification_validator/test_v1_2_real_pdb_acceptance.py \
            --basetemp=real_pdb_outputs/pytest_tmp \
            2>&1 | tee real_pdb_outputs/pytest.log
      - name: Upload acceptance evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: component-classification-v1-2-real-pdb-${{ github.run_id }}
          path: |
            real_pdb_inputs/sha256sums.txt
            real_pdb_inputs/provenance.txt
            real_pdb_outputs/
          if-no-files-found: warn
          retention-days: 30
