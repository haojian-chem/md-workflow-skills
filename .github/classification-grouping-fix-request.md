# One-time classification grouping fix

This marker opens the same-repository pull request that runs the temporary verified patch workflow. The workflow removes this file before committing the tested source and regression-test changes.
